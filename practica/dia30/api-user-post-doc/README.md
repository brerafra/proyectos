# API TEST users

Api - Users versión 3

Esta CRUD API contiene las siguientes caracteristicas:

- Uso de postgres (usando postgres Dockerizado en maquina.)
- Versión 3 domain user:
    name
    email
    card
    is_active
    is_admin
- versión con repository/service/domain/handler separados
- para la prueba de la API se empleó BRUNO